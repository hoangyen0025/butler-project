# Optional: enables AI features (suggest reply, summary, search parse).
# Without this key, those endpoints use local template fallbacks.
OPENAI_API_KEY=

# Optional model override (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Optional: verbose ticket list filter logging
# DEBUG_TICKETS=1
